import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: 'center',
    paddingVertical: 30,
  },
  logo: {
    width: 200,
    height: 200,
  },
  number: {
    fontSize: 16,
  },
});

export default styles;
